pre-commit:
  commands:
    format:
      glob: "*.{py,ipynb}"
      run: task
    test:
      run: task test
    type:
      run: task type
    lint-notebooks:
      run: task lint-notebooks
    update:
      run: git update-index --again